server {
        listen 8000;
        ServerName {{ domainname }}:8000

        root /home/{{ username }}/{{ domainname }};


        server_name {{ domainname }};

        location / {
                try_files $uri $uri/ =404;
        }
}
