---
  - hosts: alina
    tasks:
      - name: Create a login user
        user:
          name: "{{ username }}"
          password: "{{ password }}"
          state: present
      - name: ansible create directory example
        file:
          path: "/home/{{ username }}/{{domainname}}"
          state: directory
...
