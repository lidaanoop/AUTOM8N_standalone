---
  - hosts: alina
    tasks:
      - name: Create a login user
        user:
          name: "{{ name }}"
          password: "{{ password }}"
          state: present
      - name: ansible create directory example
        file:
          path: /home/"{{ name }}"/hello
          state: directory
...
